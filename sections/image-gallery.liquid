<style>

.section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

      .image-slider-new {
        display: block;
        height: 200px;
      }
      
    .image-slider-new svg, .image-slider-new img{
    width: 100%;
    object-fit: cover;
    height: 100%;
}

      @media screen and ( min-width: 768px ) {
        .image-slider-new {
          height: 400px;
        }
      }

</style>

<div class="nerd-container section-{{ section.id }}-padding color-{{ section.settings.color_scheme }}">
    <div class="{% unless section.settings.full_width %}nerd-page-width {% endunless %}">
      <div class="nerd--title-main scroll-trigger animate--slide-in {% if section.settings.heading_center %} text-center {% endif %} ">{{ section.settings.title }}</div>
  
      <div class="nerd-content nerd-page-width">
<div class="main-carousel" data-flickity='{ "cellAlign": "left", "contain": true, "prevNextButtons" : true, "pageDots": true, "groupCells" : true, "freeScroll": true  }'>
{% for block in section.blocks limit: section.settings.total_to_show %}
    <div class="image-slider-new">
        {{ block.settings.image | image_url: width: block.settings.image.width | image_tag: loading: 'lazy' }}
    </div>
{% else %}
{% for i in (1..5) %}
  
  
  <div class="image-slider-new ">
    {{ 'product-1' | placeholder_svg_tag }}
    </div>
{% endfor %}
{% endfor %}
</div>
</div>


</div>
</div>

{% schema %}
  {
    "name": "Image Gallery",
    "settings": [
      {
        "type": "inline_richtext",
        "id": "title",
        "default": "Image Gallery",
        "label": "Title"
      },
      {
        "type": "checkbox",
        "id": "full_width",
        "default": false,
        "label": "Full Width"

      },
      {
        "type": "range",
        "id": "total_to_show",
        "min": 1,
        "max": 15,
        "default": 5,
        "step": 1,
        "label": "Total Items to show"
      }, {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "t:sections.all.colors.label",
        "info": "t:sections.image-banner.settings.color_scheme.info",
        "default": "background-1"
      },
      {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }


    ],
    "blocks": [
      {
        "type": "image",
        "name": "Image",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Image Gallery"
      }
    ]
  }
{% endschema %}